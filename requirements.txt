# Python 의존성 (Motia가 이 파일을 사용)
# 주의: torch는 용량이 크므로 설치 시간이 오래 걸릴 수 있음

# PyTorch
torch

# TimesFM 최신 버전 (GitHub에서 직접 설치)
timesfm @ git+https://github.com/google-research/timesfm.git

# JAX (CPU)
jax[cpu]

# Data Processing
numpy
pandas

# Utilities
utilsforecast
huggingface_hub
tensorflow
scikit-learn
