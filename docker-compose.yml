version: '3.8'

services:
  app:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/python_modules
    environment:
      - PORT=7860
    command: npm run dev
