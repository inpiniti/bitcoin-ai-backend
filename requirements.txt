# Python 의존성 (FastAPI 기반)
# 주의: torch/timesfm은 용량이 크므로 설치 시간이 오래 걸릴 수 있음

# ── Web Framework ────────────────────────────────
fastapi
uvicorn[standard]
httpx
websockets
aiohttp
supabase

# ── YouTube MP3 ───────────────────────────────────
yt-dlp

# ── Core ML Libraries (Versions pinned to avoid backtracking) ────
# numpy 1.26.4는 uni2ts(~=1.26.0), timesfm(>=1.26.4), chronos(<3,>=1.21) 모두 만족
# numpy 2.x 모델 pickle은 services/numpy_compat.py 에서 shim으로 처리
torch>=2.0.0
jax[cpu]>=0.4.0
numpy==1.26.4
pandas>=2.0.0,<3.0.0

# ── TimesFM (Google) ─────────────────────────────
timesfm @ git+https://github.com/google-research/timesfm.git

# ── Time-series Forecast Models ──────────────────
# Amazon Chronos-2
chronos-forecasting @ git+https://github.com/amazon-science/chronos-forecasting.git
# Salesforce Moirai
uni2ts @ git+https://github.com/SalesforceAIResearch/uni2ts.git
einops

# ── ML Algorithms & Tools ────────────────────────
utilsforecast
scikit-learn
xgboost
stable-baselines3>=2.3.0
gymnasium>=0.29.0

# ── Data & Misc ──────────────────────────────────
beautifulsoup4
huggingface_hub
tensorflow>=2.15.0
joblib
yfinance
lxml
google-generativeai
